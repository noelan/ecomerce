<form method="post" action="{{ path('cloth_delete', {'id': cloth.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	    <input type="hidden" name="_method" value="DELETE">
	    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cloth.id) }}">
	    <button class="btn btn-danger" style="width:10rem;">Supprimer</button>
</form>